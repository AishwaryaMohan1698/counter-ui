import { CompartmentChartDataItem } from './compartment-chart-data-item';

describe('CompartmentChartDataItem', () => {
  it('should create an instance', () => {
    expect(new CompartmentChartDataItem()).toBeTruthy();
  });
});
